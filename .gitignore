node_modules/
.DS_Store
Thumbs.db
*.log
src/ocr/tessdata/*.traineddata
src/ocr/_test_*

# Intermediate OCR working directories (page images + raw first-pass text) —
# large and fully reproducible from the source PDFs via src/ocr/ocr_pdf.py;
# only the final book.json (built from these) is meant to be tracked.
books/*/_ocr*/
